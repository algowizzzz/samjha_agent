# R0 Policy Document Summary

This policy document outlines access control requirements for ACME Bank, including:

- Enforcing multi-factor authentication (MFA) for all privileged accounts
- Reviewing access for admin groups on a monthly basis
- Implementing just-in-time access for production systems where feasible

The policy also includes an access review schedule table that defines the frequency and evidence required for different user groups.

## Obligations

| Type | Statement | Section |
| --- | --- | --- |
| MUST | Enforce MFA for all privileged accounts. | Requirements |
| MUST | Review access for admin groups monthly. | Requirements |
| SHOULD | Implement just-in-time access for production systems where feasible. | Requirements |

## Gaps

| Severity | Gap | Related Obligation |
| --- | --- | --- |
| Medium | The policy does not define what 'feasible' means in the context of implementing just-in-time access. This could lead to ambiguity in implementation. | Implement just-in-time access for production systems where feasible. |

## Questions

1. Can you provide more guidance on what criteria should be used to determine if just-in-time access is 'feasible' for a given production system?