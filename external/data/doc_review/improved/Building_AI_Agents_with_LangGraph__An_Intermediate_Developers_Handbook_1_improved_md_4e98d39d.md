## Table of Contents
  - [1. Foundations of Agentic Graphs with LangGraph](#sec-ce9dd6a0a9)
  - [At the heart of LangGraph are three core primitives](#sec-00f05678d6)
  - [Using graphs (with cycles) instead of a simple loop brings several benefits](#sec-02a0198047)
  - [Some key features and components include](#sec-1f614e11ed)
  - [2. Modeling State & Control Flow](#sec-dc887ed782)
  - [MessagesState {"messages"](#sec-d0a2cb59d7)
  - [a user profile or some session info, you could define](#sec-95fdfb94f2)
  - [class MyAgentState(TypedDict)](#sec-829b1eb80e)
  - [When building the , you will specify this as the state schema](#sec-794803d180)
  - [to add each step of logic](#sec-80786c02bd)
  - [END](#sec-720a160895)
  - [def call_model(state: MessagesState)](#sec-092ea0848a)
  - [def call_tools(state: MessagesState) -> Literal["tools", END]](#sec-5ad1e6061b)
  - [if last_message.tool_calls](#sec-32eecb0bac)
  - [Let’s unpack what we did](#sec-e95fa5983e)
  - [END](#sec-720a160895)
  - [For instance](#sec-5a55a1016c)
  - [for chunk in agent.stream(input_state, stream_mode="values")](#sec-a872a543f3)
  - [END](#sec-63893f58ae)
  - [When modeling control flow, consider](#sec-99e9c6c070)
  - [3. Tools & External Actions](#sec-07f161b1e9)
  - [Important points about tool calls](#sec-62c0637a85)
  - [LangGraph by extension) makes this easy via the decorator](#sec-2b610c0cd3)
  - [def get_weather(city: str) -> str](#sec-b9fdd6a3e9)
  - [if "location" in resp](#sec-19e409087c)
  - [else](#sec-760d331a21)
  - [Tool design tips](#sec-0817b844c6)
  - [the tools when the LLM requests them](#sec-f6ae200d41)
  - [already wired (essentially what we built manually in Module 2). For example](#sec-7dca23539b)
  - [Let’s revisit a concrete example with code (simplified for clarity)](#sec-0ffda90256)
  - [def search_web(query: str) -> str](#sec-feba393c3a)
  - [Best Practices for Tools](#sec-d59b33e54e)
  - [Module 6) so the LLM doesn’t misuse tools. For instance, if you have a tool](#sec-cb760e84e5)
  - [module). LangGraph allows tools to read/write state – e.g., a tool could update](#sec-c370729f25)
  - [def define_word(word: str) -> str](#sec-c428845525)
  - [4. Persistence, Checkpointing & Replay](#sec-746f0eddb0)
  - [agent.invoke(input, config={"configurable": {"thread_id"](#sec-120c5fe21f)
  - [Enabling persistence: To save checkpoints, you compile your graph with a](#sec-59a333b3ec)
  - [agent.invoke(None, config={"configurable"](#sec-44d4fb1add)
  - [control for building things like](#sec-7ff2267759)
  - [5. Memory Architectures (Short-Term, Episodic, Semantic)](#sec-665e97a40b)
  - [state from one to the next](#sec-c71656ed2a)
  - [Common strategies (LangGraph provides utilities for these )](#sec-80af14ff98)
  - [In practice, to implement trimming in LangGraph, you can do](#sec-728c7db35a)
  - [def my_pre_model_hook(state)](#sec-d4cd4d831c)
  - [or instead of a checkpointer)](#sec-f6bd3a6197)
  - [def call_model(state: MessagesState, config: RunnableConfig, *, store](#sec-1abf1b8717)
  - [BaseStore)](#sec-57e9692fb0)
  - [# If user asked to remember something](#sec-da82c9f675)
  - [if "remember" in last_user_msg.lower()](#sec-69730392aa)
  - [store.put(("memories", user_id), str(uuid.uuid4()), {"data"](#sec-15100d174d)
  - [LangGraph’s can be configured with an embedding index . For example](#sec-cd19f20314)
  - [6. Human-in-the-Loop (HITL) & Guardrails](#sec-980d0f62e4)
  - [We could do](#sec-65cb72cb4f)
  - [To resume, the human triggers the continuation, which might be something like](#sec-67df561871)
  - [Commands](#sec-c271fbe750)
  - [allows a human to see “Agent is about to execute](#sec-c77065d471)
  - [7. Streaming, Observability & Developer UX](#sec-b088d5b139)
  - [for chunk in agent.stream(input, stream_mode="values")](#sec-4061b35c6a)
  - [To enable](#sec-e7c99472a0)
  - [A few things make LangGraph nicer to work with as a developer](#sec-5f99b6ad2f)
  - [part of LangChain’s platform, not open source, it indicates the direction: a GUI to design and test](#sec-5829a827cc)
  - [partial content](#sec-323ed08260)
  - [for message, metadata in agent.stream(input, stream_mode="messages")](#sec-bcfcc84f32)
  - [if message.role == "ai"](#sec-7c4f1626eb)
  - [8. Multi-Agent & Orchestration Patterns (Advanced)](#sec-8b8e409c9f)
  - [9. Evaluation, Testing, Safety & Cost Control](#sec-42a5b3f3cd)
  - [module can help here – you can programmatically compare outputs and mark pass/fail. - Regression](#sec-c29067f06a)
  - [if callback_handler.total_cost > MAX_COST](#sec-61d747d533)
  - [10. Production Deployments & Capstone Use Cases](#sec-b33191dad6)

## 1. Foundations of Agentic Graphs with LangGraph
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## At the heart of LangGraph are three core primitives
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Using graphs (with cycles) instead of a simple loop brings several benefits
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Some key features and components include
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 2. Modeling State & Control Flow
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## MessagesState {"messages"
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## a user profile or some session info, you could define
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## class MyAgentState(TypedDict)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## When building the , you will specify this as the state schema
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## to add each step of logic
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## END
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def call_model(state: MessagesState)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def call_tools(state: MessagesState) -> Literal["tools", END]
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## if last_message.tool_calls
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Let’s unpack what we did
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## END
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## For instance
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## for chunk in agent.stream(input_state, stream_mode="values")
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## END
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## When modeling control flow, consider
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 3. Tools & External Actions
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Important points about tool calls
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## LangGraph by extension) makes this easy via the decorator
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def get_weather(city: str) -> str
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## if "location" in resp
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## else
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Tool design tips
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## the tools when the LLM requests them
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## already wired (essentially what we built manually in Module 2). For example
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Let’s revisit a concrete example with code (simplified for clarity)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def search_web(query: str) -> str
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Best Practices for Tools
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Module 6) so the LLM doesn’t misuse tools. For instance, if you have a tool
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## module). LangGraph allows tools to read/write state – e.g., a tool could update
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def define_word(word: str) -> str
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 4. Persistence, Checkpointing & Replay
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## agent.invoke(input, config={"configurable": {"thread_id"
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Enabling persistence: To save checkpoints, you compile your graph with a
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## agent.invoke(None, config={"configurable"
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## control for building things like
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 5. Memory Architectures (Short-Term, Episodic, Semantic)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## state from one to the next
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Common strategies (LangGraph provides utilities for these )
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## In practice, to implement trimming in LangGraph, you can do
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def my_pre_model_hook(state)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## or instead of a checkpointer)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## def call_model(state: MessagesState, config: RunnableConfig, *, store
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## BaseStore)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## # If user asked to remember something
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## if "remember" in last_user_msg.lower()
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## store.put(("memories", user_id), str(uuid.uuid4()), {"data"
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## LangGraph’s can be configured with an embedding index . For example
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 6. Human-in-the-Loop (HITL) & Guardrails
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## We could do
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## To resume, the human triggers the continuation, which might be something like
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## Commands
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## allows a human to see “Agent is about to execute
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 7. Streaming, Observability & Developer UX
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## for chunk in agent.stream(input, stream_mode="values")
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## To enable
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## A few things make LangGraph nicer to work with as a developer
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## part of LangChain’s platform, not open source, it indicates the direction: a GUI to design and test
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## partial content
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## for message, metadata in agent.stream(input, stream_mode="messages")
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## if message.role == "ai"
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 8. Multi-Agent & Orchestration Patterns (Advanced)
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 9. Evaluation, Testing, Safety & Cost Control
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## module can help here – you can programmatically compare outputs and mark pass/fail. - Regression
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## if callback_handler.total_cost > MAX_COST
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State

## 10. Production Deployments & Capstone Use Cases
Objectives: In this module, you will learn what LangGraph is and why it was created. We’ll cover the limitations of basic loop-based agents, and how LangGraph introduces agentic graphs – cyclic workflows that empower more complex, flexible agent behavior . You’ll become familiar with 1 2 LangGraph’s key primitives (nodes, edges, and state) and understand how they form a stateful graph that underpins an agent’s reasoning process . 3 4 LangChain’s Agent Executor vs. LangGraph Before LangGraph, LangChain’s standard agent executor loop handled decisions and tool use in a fixed pattern. This design was powerful but rigid – every agent followed the same step-by-step loop with limited flexibility . For simple Q&A or single-turn tasks, that might suffice, but complex, multi-step 1 workflows demanded more adaptability. LangGraph was introduced to address these limitations . 5 LangGraph is an advanced library in the LangChain ecosystem that enables cyclic decision processes . Unlike LangChain’s linear directed acyclic graph (DAG) chains, LangGraph workflows can include 6 cycles – meaning an agent can loop through reasoning steps multiple times, revisit tools, and make dynamic decisions as conditions change . This cyclic capability is essential for agentic behavior, 7 8 where an AI agent may need to reflect, backtrack, or iterate on a plan . 8 Key idea: LangGraph treats an agent’s decision-making flow as a graph instead of a fixed loop. Nodes in the graph represent operations (LLM calls, tool calls, etc.), and edges represent possible transitions or control flow. Crucially, cycles in the graph allow an agent to re-enter a prior step based on new information or updated state . This unlocks more complex behaviors like retrying after an error, 7 9 refining a query, or multi-turn planning that adapts over time. Core Concepts: Nodes, Edges, and State
